PORT=5000
JWT_SECRET=change-me
JWT_EXPIRES_IN=7d
CLIENT_ORIGIN=http://localhost:3000

# Empty = embedded PGlite (default for local API)
# Or real Postgres (schema from database/schema.sql):
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/shelfmark
DATABASE_URL=
